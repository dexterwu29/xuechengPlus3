# 学成在线三代 - Docker Compose 环境变量
# 复制本文件为 .env 并填入实际密码
# cp .env.example .env

MYSQL_ROOT_PASSWORD=你的MySQL密码
REDIS_PASSWORD=你的Redis密码
MINIO_PASSWORD=你的MinIO密码
RABBITMQ_PASSWORD=你的RabbitMQ密码

# Nacos 鉴权密钥，必须 Base64 编码（≥32 字符）
# 生成命令：openssl rand -base64 32
NACOS_AUTH_TOKEN=
