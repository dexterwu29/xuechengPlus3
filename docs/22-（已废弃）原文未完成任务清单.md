# 学成在线 xuechengPlus3 - 原文未完成任务清单

> 基于用户提供的原文需求整理，已完成项已跳过。

---

## 已完成（可跳过）

| 模块 | 项 | 状态 |
|------|-----|------|
| 第一 | 导航与权限（管理页面仅 teacher/admin、菜单结构） | ✅ |
| 第二 2.2 | 封面上传 ≤2MB、后端 multipart 配置 | ✅ |
| 第三 3.1 | QQ/微信/手机必填、手机号 1[3-9]\d{9} 校验 | ✅ |
| 第四 4.1 | 教学大纲布局重设计（左树右媒资） | ✅ |
| 第四 4.2 | 当页预览（视频/PDF/文档） | ✅ |
| 第四 4.3 | 试看功能 | ✅ |
| 第四 4.4 | 教学计划为空禁止下一步、有子节禁止删章 | ✅ |

---

## 未完成任务

### 第六：课程预览 403 修复 【P0】 ✅ 已完成

**实现**：新增前端路由 `/courses/:id/preview` 与 `CoursePreviewView.vue`，通过 API 获取课程数据渲染预览；`doPreview()` 改为 `window.open('/courses/:id/preview')`，避免后端 Thymeleaf 403。

---

### 第七：管理员/教师审核流程 【P1】 ✅ 已完成（部分）

**已实现**：
- **管理员/超级管理员** 提交：直接 已提交+已审核+已发布（`submitAndPublish`）
- **教师** 提交：草稿 → 待审核
- **已发布课程修订**：教师可再次提交进入待审核（202004→202003）

**待实现（需 course_publish 表）**：
- 已发布课程修改后，用户继续看到旧内容，直至管理员审核通过。当前无 `course_publish` 表，公开接口直接读 `course_base`，无法实现“修改不影响用户”。

---

### 第二 2.1/2.3：多封面 【P3】

**需求**：
- 最多 3 张封面图
- 可编辑者选择 1 张作为默认展示（列表/详情主图）
- 其余最多 2 张仅出现在课程详情页
- 课程详情页需预留多图展示区域

**数据模型**：`cover_pics`（JSON 数组）、`default_cover_index`（默认图索引）

---

### 第三 3.2：机构/讲师/销售备案管理 【P3】

**需求**：
- 机构、讲师、销售信息需在平台备案
- 管理后台需有【机构管理】【讲师管理】【销售管理】入口
- 支持信息备案与审核

---

### 第五：媒资路径与 MD5 秒传 【P3】

**需求**：
- 路径格式：`{bucket}/{md5[0]}/{md5[1]}/{md5}/{md5}_{原文件名}.{后缀}`
- 秒传：DB 有 MD5 且 MinIO 有对应文件 → 直接返回成功，无需再传

---

## 实施顺序建议

1. **第六**：预览 403（影响使用）
2. **第七**：审核流程（核心业务）
3. **第二**：多封面
4. **第三 3.2**：备案管理
5. **第五**：媒资路径与秒传
